use strict;
use warnings;
use Test::More;
use Future::Q;

note("------ tests for try() method");

fail("TODO cases: normal return, die, return single Future (pending, fulfilled, rejected, cancelled), return with no arg, return list of Futures, return undef");

done_testing();
